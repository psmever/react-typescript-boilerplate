import tw from 'twin.macro';

export const TestStyle = {
    Main: {
        Container: tw.div`flex flex-col w-full h-screen items-center justify-center`,
        Hello: tw.div`flex`,
        EtcWapper: tw.div`flex`,
    },
};

export default TestStyle;
